import { Alert } from "react-bootstrap";

function AllNotFilled() {
  return (
    <Alert variant="warning">
      Rekomendasi tidak akan ditampilkan sehingga semua data diisi!
    </Alert>
  );
}

export default AllNotFilled;
